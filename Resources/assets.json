{
  // The paths to search thru (in order) for the independent sprites
  "independentSpritesSearchPaths": [
    "assets/sprites/independentSprites",
    "config/sprites/independentSprites",
    "config/facePNGs",
    "config/devOnlySprites/independentSprites"
  ],

  // The paths to search thru (in order) for the sprite sequences
  "spriteSequencesSearchPaths": [
    "assets/sprites/spriteSequences",
    // This is in Cozmo, but not Vector
    "assets/faceAnimations",
    // This is in 1.7, not 1.6
    "config/sprites/spriteSequences",
    // This is in 1.6 not 1.7
    "config/devOnlySprites/spriteSequences"
  ]
}
